const config = require("../../config.js");
const request = require("../../util/request");

let app = getApp()

Page({

  /**
   * 页面的初始数据
   */
  data: {
    opText: "发送验证邮件"
  },

  sendVerifyMail: function(event) { 
    
  },
 
})