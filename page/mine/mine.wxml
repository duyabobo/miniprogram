<view class="my-info">
  <image class='head-img' src="{{headImg}}"></image>
</view>

<view wx:for="{{mainGroupList}}" wx:for-item="func_group" wx:key="id" class="func-group-list">
  <button class="func-group" open-type="{{item.openType}}" wx:for="{{func_group}}" wx:key="id" data-url="{{item.url}}" data-need_login="{{item.needLogin}}" bindtap="{{item.bindFuncName}}">
    <view class="func-name">{{item.name}}</view>
    <view class="navigator-arrow"></view>
  </button>
</view>
